# 第1世代NNUEの記録 — 深さ4で作って、負けた

2026年8月10日。`docs/nnue.md` の手順を最初から最後まで1周回した記録。結果は**失敗**で、gen1は手書き評価に固定深さ6で 1-399-0(0.3%)。

失敗そのものより、**測定を4回間違えたこと**のほうが記録する価値がある。どれも例外を出さず、もっともらしい数字を返してきた。同じ罠は次の世代でも同じ形で待っている。

## 結論から

深さ4の自己対局データでNNUEを作ってはいけない。

深さ4の探索スコアは手書き評価そのものとほとんど同じ量なので、それを蒸留しても手に入るのは**手書き評価の劣化コピー**でしかない。そこに実行コストが乗る以上、勝てる道理がない。ラベルが手書き評価より多くの情報を持つ深さ — 6以上 — から始めること。

## 何を回したか

| | |
|---|---|
| データ生成 | `--games 20000 --depth 4 --concurrency 16`、約1時間、**235万局面**(197MB) |
| 学習 | `--steps 400000`(既定)、バッチ8192、RTX 4070 Ti で約2時間、57 steps/s |
| エポック数 | **1393**(40万step × 8192 ÷ 235万) |
| 訓練loss | 0.0402 → **0.0000435** |
| 量子化検証 | 5000局面で完全一致(AVX2) |
| 強さ | 固定深さ6・400局で **1 - 399 - 0**、Elo -800(下限に張り付き) |

`--steps 400000` は既定値だが、`docs/nnue.md` が想定していた1000万局面向けの数字だった。235万局面に対しては1393エポックになる。

## 測定の罠

### 1. EvalFileの読み込み失敗は「互角」という顔で返ってくる

最初に `--movetime 200` で400局回して 47.8%(Elo -15.6 ± 33.5)を得た。**この対局はNNUEを使っていない。**

エンジンは `EvalFile` の読み込みに失敗すると、`info string` で理由を言って手書き評価のまま対局を続ける。これはGUIのパス打ち間違いで対局そのものを失わないための設計で、正しい。ただし測定に使うと、**失敗した測定が「手書き評価 対 手書き評価」= 50% という、いかにも「有意差なし」に見える結果になる。**

対策: `luna-match` の出力に `info string eval nnue ...` が出ていることを毎回確認する。

### 2. bashのバックスラッシュ

失敗の原因はこれだった。

```bash
--option EvalFile=D:\Program\git\luna-shougi\nets\gen1.nnue   # 壊れる
```

引用符の無いバックスラッシュはbashがエスケープとして食う。エンジンに届くのは `D:Programgitluna-shouginetsgen1.nnue`。スラッシュ区切りで書けば起きない。

```bash
--option EvalFile=D:/Program/git/luna-shougi/nets/gen1.nnue   # 正しい
```

同じ理由で `bench` の比較も1度目は無効だった。**見分け方はノード数**で、深さを固定したベンチのノード数が両者で1ノードも違わないなら、それは同じ評価関数を2回測っている。

### 3. `eval` コマンドの出力は評価関数によって形が違う

```
info string eval material 123 pst -45 king 0 tempo 30 total 108   # 手書き
info string eval nnue -137 net D:\...\gen1.nnue (avx2)            # NNUE
```

「4番目のトークンを取る」と書くと、手書き側からは `material` の値が出てくる。**`material` は先手視点、`total` は手番視点**(`src/search/eval.cpp` の `terms.total`)なので、これを混ぜると先手番と後手番で相関が +0.807 / -0.807 に割れる。手番の符号バグが入っているようにしか見えないが、単に違う数字を比べている。

### 4. 訓練データの局面で汎化を測っていた

未知局面での挙動を見るつもりで `data/gen1.sfen` を使った。これは学習に使った局面そのものなので、何を測っても「暗記できているか」しか分からない。

別シードで生成し直したホールドアウトが要る。

```bash
./build-msvc/Release/luna-datagen.exe --out data/holdout.bin --sfen-out data/holdout.sfen --games 60 --depth 4 --seed 999 --concurrency 8
```

### 5. 検証lossは学習と同じ目標で測る

損失が最適化しているのは `0.7 × スコア勝率 + 0.3 × 勝敗`(`--lambda-score` 既定0.7)であって、スコア勝率だけではない。スコア勝率だけと比べると検証lossが0.016と出て、訓練lossの360倍に見える。**過学習の有無について正反対の結論が出る。**

目標を直しても、まだ揃っていなかった。訓練lossはfloatモデルのもので、エンジンに評価させて出した検証lossは量子化後のもの。この2つを並べると0.0000435対0.000219で「5倍の開き」に見えるが、差の中身はほとんど量子化(下の節)。**同じfloatモデルで測ると0.000024で、訓練lossより低い。**比べる2つの数字は、同じ目標かつ同じ精度で測ったものでなければならない。

## 測定結果

未知局面(seed 999)で、各評価が教師ラベル(深さ4の探索スコア、手番視点)をどれだけ当てられるか。`training/baseline.py` と `training/holdout.py` で測り直した数字。

エンジン経由(量子化後、実際に指すもの):

| | ラベルとの相関 | 残差sd |
|---|---|---|
| 手書き評価(静的) | +0.939 | **473** |
| gen1 (40万step, 1393エポック) | +0.895 | 604 |
| gen1-short (3万step, 104エポック) | +0.898 | 596 |

学習側(floatモデル、`--val-data` が出すもの):

| | 訓練loss | 検証loss | 残差sd |
|---|---|---|---|
| gen1 (40万step) | 0.0000435 | **0.000024** | 600 |
| gen1-short (3万step) | 0.000325 | 0.000894 | 599 |

読み方は3つ。

**過学習はしていない。** gen1は検証lossが訓練lossより**低い**。1393エポックという数字は多すぎるように見えるが、このデータとこのネットの組み合わせでは何も起きていない。3万stepに減らした側は検証lossが37倍悪く、短く回すのは改善ではなかった。

**ネットワークは自分の教師より下手。** 手書き評価の残差sdが473、NNUEはどちらも約600。深さ4の探索スコアを、その探索を駆動した手書き評価自身より正確には予測できていない。ステップ数を変えても動かない(604と596)。**ここが失敗の本体。**

**残差sdと検証lossは別のことを言う。** gen1とgen1-shortは検証lossで37倍違うのに、残差sdは1%も違わない。勝率の二乗誤差は飽和した両端が支配するので、点数としての精度はほとんど見ていない。外部の基準(473)と比べられるのは残差sdのほうで、合格判定にはこちらを使う。

### 量子化が35点ずれていた — 入っていたのはL1とL2の重みの丸め

floatモデルの出力とエンジンが返す点数を同じ局面で比べると、**平均 -34.6点、sd 33.0、最大297点**(歩=90)。

sdのほうは丸め誤差として理解できる。**平均が0でないほうが問題**で、これは量子化がノイズではなく偏りを入れているということ。手番視点の評価に定数を足すのは、negamaxでは1手ごとに符号が反転するので、実質0.4歩弱の手番ボーナス(またはペナルティ)を全ノードに撒くのと同じで、指し手は変わる。

**`training/verify.py` はこれを検出できない。** あちらはPython側の量子化とC++側の量子化を突き合わせるので、両方が同じ偏りを持っていれば「完全一致」と出る。実際gen1でも5000局面で完全一致していた。float と量子化後の比較は別の検査で、これが `training/drift.py`(下記)。

#### どこで入っているか

量子化の要素を1つずつ有効にして、800局面での平均のずれを測った(単位はエンジンの点数)。全部offにするとfloatモデルと一致する(最大0.001点)ので、各行はその要素だけの寄与。

| 有効にした要素 | 平均 | sd |
|---|---|---|
| L2の重みの丸め | **-26.7** | 16.4 |
| L1の重みの丸め | **-12.5** | 27.0 |
| 出力層の重みの丸め | +5.1 | 2.5 |
| 特徴変換器の重みの丸め | -0.3 | 12.7 |
| 6bit右シフトの切り捨て | -0.2 | 6.2 |
| 各層のバイアスの丸め | ±0.05 | ~0 |
| 出力の16での切り捨て除算 | -0.00 | 0.6 |

**疑っていた2つはどちらも外れ。** 右シフトは確かに floor で1層につき0.5ずつ負に倒れるが、ここでは0.2点にしかならない(丸めに直しても -34.6 が -34.3 になるだけ)。バイアスのスケールは3層とも重みと整合していて、ずれは0.05点。

**入っているのはL1とL2の重みの丸め。** 最近接丸めなので重み1個あたりの偏りは無い。偏るのは層の単位で、理由がある。L1もL2も密で、**どの局面でも全入力を使う**。丸め誤差は「局面によってほとんど動かない活性ベクトル」に対する固定の線形汎関数になり、ノイズではなくほぼ定数として出てくる。特徴変換器が-0.3で済んでいるのは、局面ごとに違う38行しか足さないから — こちらの誤差は本当に均される。

#### 「量子化の偏り」ではなく「このネットワークの引き」

スキームが系統的に負に倒れているのではない。同じ学習の途中のチェックポイントで測ると符号が変わる。

| checkpoint | 平均 | sd |
|---|---|---|
| gen1 50k step | +14.4 | 24.7 |
| gen1 150k | -11.8 | 27.1 |
| gen1 250k | +23.2 | 30.8 |
| gen1 350k | +9.0 | 36.0 |
| gen1 400k(採用したもの) | **-34.6** | 33.0 |
| gen1-short | -9.9 | 30.2 |
| smoke | -8.7 | 12.7 |

丸めにディザを入れて(`round(x + u)`, `u ~ U(-0.5, 0.5)`、これも不偏)同じネットワークを24通り量子化すると、平均は -75 から +36 まで sd 29 で散らばる。最近接丸めの -34.6 はその散らばりのちょうど1σぶん外れたところに落ちているだけで、特別な位置ではない。

つまり **-34.6 はスキームの欠陥ではなく、このネットワークが引いた定数**。±30点の分布から1つ引いているだけで、丸め方をどう変えても引き直しにしかならない。ただし引いてしまった以上そのネットワークにとっては定数であり、上に書いた手番ボーナスの話はそのまま成立する。**変わるのは直し方で、「丸め方を直す」ではなく「測って引く」になる。**

#### 直した

`quantize.correct_biases`。学習データ4096局面をfloatモデルと量子化ネットワークの両方に通し、各層の平均誤差をその層の整数バイアスから引く。前の層から順に直す(L1を直すとL2が見るものが変わる)。バイアスはint32で桁が余っているので費用はゼロ。右シフトの切り捨ても次の層の測定に含まれるので一緒に吸われる。

**エンジンは1行も変わっていない。** 変わるのはファイルに入る数字だけなので、補正したネットワークでも `training/verify.py` は2000局面で完全一致する(確認済み)。

補正は学習データ(`data/gen1.bin`)で当て、測ったのはホールドアウト800局面:

| | 平均 | sd | mean\|d\| | max\|d\| |
|---|---|---|---|---|
| 補正なし | -31.7 | 30.2 | 34.8 | 156 |
| 補正あり | **+0.6** | 27.2 | 20.0 | 113 |

sdとmaxも下がっているのは、引いたものがほぼ定数だったから当然。エンジン経由で端から端まで測っても同じ(+0.2 / 27.0)。

#### 検査を追加した

`training/drift.py`。floatモデルと量子化ネットワークを同じ局面に通し、**平均だけ**を合否に使う(既定 ±8点、歩=90)。sdは表示するが判定に使わない — int8である以上ずれるし、それは正常。この検査があれば、gen1をエクスポートした時点で落ちていた。

```bash
uv run python -m training.drift --checkpoint runs/gen1/checkpoint.pt --net nets/gen1.nnue --sfen data/holdout.sfen
```

#### 強さへの影響

同じバイナリで `EvalFile` だけを変えて対局させた。

| 条件 | 結果 |
|---|---|
| 200ms/手, 400局 (seed 4242), 8並列 | corrected 233 - 162 - 5、**+62.3 ± 34.5 Elo**, LOS 100% |
| 同上、別シードで確認 (seed 777) | corrected 222 - 176 - 2、**+40.1 ± 34.3 Elo**, LOS 98.9% |

2つの信頼区間は重なっているので、これは同じ結果を2回測ったものと見ていい。どちらも誤差幅を超えている。**35点の定数は、指し手を変えるという意味では確かに効いていた。**

ただしラベルに対する残差sdはほとんど動かない。

| | ラベルとの相関 | 残差sd | bias |
|---|---|---|---|
| gen1(補正なし) | +0.896 | 605 | -36 |
| gen1(補正あり) | +0.897 | 603 | **-4** |

`training/baseline.py` の `bias` 行には最初から出ていた(-36)。比べる相手が無かったので読めなかっただけ。**合格判定に使っている残差sdのほうは2点しか動かない。**

だから **gen1の失敗の説明は変わらない**。手書き評価の473に対して603のままで、これは相変わらず教師より下手なネットワーク。+62 Elo は「同じ弱いネットワークどうし」の比較であって、手書き評価との 1-399-0 を説明するものではない。**深さ4のデータを選んだことが失敗の本体**という結論はそのまま。

### 実行コスト

`bench 8`(両方ともパスが正しく通った状態):

| | ノード数 | 時間 | NPS |
|---|---|---|---|
| 手書き | 580,060 | 559 ms | 1,037,674 |
| NNUE | 1,107,545 | 1,831 ms | 604,885 |

コストが2つ重なる。**NPSが0.58倍**(評価1回が重い)、そして**同じ深さに着くまでのノード数が1.91倍**。合わせて深さ8への到達時間が**3.3倍**になる。

ノード数のほうが重要で、これは速度ではなく評価値の性質の話。評価が局面ごとにギザギザだとalpha-betaが枝を切れない。残差sd 604(歩=90なので約6.7歩)がそのまま効いている。フェーズ6の枝刈りマージンが手書き評価のスケールで調整されている影響もあり得る。

## なぜ深さ4では駄目なのか

深さ4の探索スコアは、手書き評価を4手先まで伝播させたものにすぎない。手書き評価との相関が高いのは当たり前で、そこには**静的評価が表現できない情報がほとんど含まれていない**。

蒸留で得られるものの上限は教師のラベルであり、その上限自体が「手書き評価とほぼ同じ」なら、うまくいっても引き分けが天井になる。そこに3.3倍の実行コストが乗るので、実際には大きく負ける。

`docs/nnue.md` の「手書き評価の間違いを覚えるだけになりやすい」という記述は、外部棋譜を使う理由として書かれていた。フェーズ6で探索が強くなったので第1世代も自己対局でよいと判断したが、**その判断が成立するのは深さを上げた場合だけ**だった。深さ4を選んだのは生成時間を6時間に収めるためで、そこが誤り。

## 次にやること

1. **深さ6で1000万局面**(約19時間)

   ```bash
   ./build-msvc/Release/luna-datagen.exe --out data/gen2.bin --sfen-out data/gen2.sfen --games 90000 --depth 6 --concurrency 16 --stop-file data/stop
   ```

2. **学習は80エポック前後を狙う**。1000万局面・バッチ8192で1エポック約1220step。

   ```bash
   uv run python -m training.train --run runs/gen2 --data data/gen2.bin --steps 100000 --export nets/gen2.nnue
   ```

3. **対局の前に残差sdを見る**。`--val-data` を付ければ学習中に出る。ただし合格ラインは深さごとに違うので、深さ6のホールドアウトで `training/baseline.py` を回して基準値を出し直すこと。473は深さ4の数字。

4. 深さ6でも基準値を切らないなら深さ8(約77時間)。それでも駄目なら、蒸留ではなく `--lambda-score` を下げて勝敗側に寄せる方向を検討する。

## 積み残し

- ~~**`training/train.py` に検証lossが無い**~~ → 入れた。`--val-data` で `val loss` と `val_resid` が2000ステップごとに出る。この記録の残差sd(手書き473、gen1が604)がそのまま学習中に見える数字になったので、**対局を待たずに失敗が分かる**。使い方は `docs/nnue.md` の「未知の局面で測る」。
- `uv run python -m training.verify --help` が日本語WindowsのCP932コンソールで `UnicodeEncodeError`(ヘルプ文字列中の全角ダッシュ)。実行自体には影響しない。
