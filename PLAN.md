# luna-shougi 開発プラン

自作将棋AIをShogiGUI(Windows)とShogiHome(Mac)で対局できる状態まで持っていくためのフェーズ分け。学習目的の自作なので、既存エンジンのコード流用はせず、参考実装として読むだけにする。

## 技術選定

先に決めを書いておく。

- エンジン本体は C++20 + CMake。NNUEのCPU推論(AVX2)の資料と参考実装がC++に集中しているのが理由。RustでもできるがSIMD周りで回り道が増える。
- 学習側は Python + PyTorch。RTX 4070 Ti(CUDA)は学習にだけ使う。
- 対局はCPU推論。NNUEはもともとCPUで速く動かすための設計なので、この要件とは相性がいい。
- GUIはUSIプロトコルで接続する。WindowsはShogiGUI、MacはShogiHome(旧ElectronShogi)。エンジン側はUSIを喋るだけで両対応になる。
- Mac側の開発と対局確認はMacBook Air M5(24GB)の実機で行う。GitHub Actionsのmacos(arm64)ランナーはビルドが壊れていないかの継続的な担保用。
- 学習はWindows側のRTX 4070 Ti(CUDA)で行う。M5でもPyTorchのMPSで学習は動くが、速度と資料の充実度でCUDA一択。M5は対局とNEON推論の検証に使う。

## モデルの使い分け

- Opus 5: バグを入れてもすぐには表面化しない領域。合法手生成、探索の枝刈り、NNUEの量子化と増分計算。ここのバグは数百局後に1回だけ出る類のもので、後から原因を探すコストが大きい。
- Sonnet 5: 仕様が明確でテストで即検証できる領域。USIプロトコル、ビルド設定、CI、学習スクリプト、自己対局ツール。

## 作業環境の使い分け

基本はWindows、フェーズ5のNEON部分だけMac。理由は3つ。

- perft検証、探索ベンチマーク、自己対局測定はCPUを長時間回し続ける作業で、7800X3D(8コア16スレッド、冷却あり)とファンレスのMacBook Airでは持続負荷の性能差が大きい
- 学習はCUDA必須なので、フェーズ5の学習スクリプトはWindows以外で動かせない
- NEONコードはx86上で実行テストできず、AVX2コードはMacで動かない。SIMD実装だけは物理的にそれぞれの機械が要る

| フェーズ | 作業環境 | 補足 |
|---|---|---|
| 0 | Windows | CIでMacビルドを最初から回す |
| 1 | Windows | perft連続実行が重い。Macビルドの破損はCIが検知 |
| 2 | Windows | 最後の対局確認だけ両方(ShogiGUIとShogiHome) |
| 3 | Windows | 自己対局測定がCPU勝負 |
| 4 | Windows | 同上 |
| 5 | Windows + Mac | 学習とAVX2はWindows、NEON推論の実装と検証だけMac |
| 6 | Windows | マルチスレッド探索は16スレッドで検証。NEONの回帰確認だけMac |

フェーズ0でCIを作っておけば、Macビルドの破損はpushのたびに自動で分かる。Macを開くのはNEONを書くときと対局確認のときだけでいい。機械の切り替えはgit経由で行う。

## フェーズ0: プロジェクト骨格(Sonnet 5)

- CMakeプロジェクト構成、clang-format、テストフレームワーク(Catch2かGoogleTest)
- GitHub ActionsでWindows(MSVC)とmacOS(arm64)のビルド+テスト
- USIの最低限応答だけのダミーエンジン。usi、isready、quitに答える
- 完了条件: ShogiGUIがエンジンとして認識し、エンジン設定画面が開く

目安1週間。ここは退屈だが、CIを最初に作っておかないとMac対応が後で崩れる。

## フェーズ1: 盤面表現と合法手生成(Opus 5)

- 局面クラス。盤、駒台、手番、Zobristハッシュ
- SFENの読み書き
- 指し手生成。成りと不成、駒打ち、二歩、打ち歩詰め、行きどころのない駒、王手放置の除外
- perftで検証。平手初期局面と既知のテスト局面で、公開されているノード数と完全一致させる

全フェーズで一番バグが出るのがここ。打ち歩詰めと不成の扱いは特に間違えやすい。perftが1ノードでもずれたら先に進まないこと。ここが崩れると後の全部が信用できなくなる。目安2〜3週間。

## フェーズ2: ShogiGUIで対局(Sonnet 5)

- position、go、bestmoveの実装。goのbtime/wtime/byoyomiのパース
- 思考部はまず合法手ランダムでいい
- ShogiGUI(Windows)とShogiHome(MacBook Air M5)に登録して人間対エンジンで1局指す

完了条件: 両OSのGUIで投了まで対局が成立する。ランダム指しなので普通に勝てるはず。ここが最初のマイルストーンで、以降は思考部の入れ替えだけになる。目安1週間。

## フェーズ3: 探索(Opus 5)

- 駒割だけの評価関数
- alpha-beta探索と反復深化
- 静止探索(取り合いの読み切り)
- 置換表
- ムーブオーダリング。MVV-LVA、killer move、history heuristic
- 時間管理。秒読みと持ち時間の配分

各機能を入れるたびに、入れる前のバージョンと自己対局して勝率を測る。効果測定の習慣をここで作る。この時点でアマ級位者くらいには指せるようになる。目安3〜4週間。

## フェーズ4: 手作り評価関数と計測基盤(Sonnet 5)

- piece-square table(駒の位置ごとの価値)
- 玉の安全度、手番ボーナスなど
- 自己対局ツール。2つのエンジンバイナリを連続対局させ、勝率とレート差を出すスクリプト

評価関数そのものより計測基盤のほうが大事。これがないと以降の改善が全部勘になる。NNUE移行後も教師データ生成とレート測定でずっと使う。目安2週間。

## フェーズ5: NNUE評価関数(Opus 5)

- HalfKP特徴量と増分計算(差分更新)
- ネットワークは小さめから。256x2-32-32-1あたり
- 教師データ: floodgateの棋譜から開始し、後でフェーズ3エンジンの深い探索による自前生成に切り替える
- PyTorchで学習し、int16/int8に量子化してバイナリ出力
- C++側でSIMD推論。WindowsはAVX2、Mac(arm64)はNEON。M5実機で対局するので、NEON版はフォールバックではなく本実装として書く
- RAM 32GBなので教師データはメモリに全部載せず、ストリーミングで読む
- 学習は任意のタイミングで停止・再開できること。止めた続きから再開したとき、止めずに回した場合と重みが完全に一致すること

学びどころが一番多いフェーズ。量子化後にPython側の推論結果とC++側の推論結果を突き合わせるテストを必ず書く。ここがずれていると弱い理由が永遠に分からなくなる。目安1〜2ヶ月。

停止・再開は当初のプランになかったが、学習が数時間から数日かかる以上、機械を占有し続けるか進捗を捨てるかの二択になるのは実用に耐えない。重みだけでなくオプティマイザの状態・データの読み位置・乱数状態まで保存する。手順は `docs/nnue.md`。

## フェーズ6: 強くする反復(Sonnet 5中心、詰まったらOpus 5)

- 枝刈りの追加。null move pruning、LMR、futility pruning
- Lazy SMPによるマルチスレッド探索(7800X3Dの8コアを使う)
- ponder(相手番思考)
- 定跡の作成と利用
- データ生成、学習、自己対局測定のループを回す

ここからは終わりがない。1つ入れては測る、の繰り返し。枝刈りとマルチスレッドのバグ調査はOpusに切り替えたほうがいい。

フェーズ5で分かったこととして、静止探索の枝刈り(delta pruning、SEE)はここで思っていたより優先度が高い。stand-patだけだと、持ち駒の多い中盤局面が深さ3で35万ノード(平手初期局面は2,472ノード)まで膨らむ。教師データ生成の速度がこれで決まってしまうので、大量にデータを作る前にここだけ先にやる価値がある。詳細は `docs/nnue.md`。

## 参考資料

- USIプロトコル仕様: http://shogidokoro.starfree.jp/usi.html
- やねうら王(NNUE実装の参考): https://github.com/yaneurao/YaneuraOu
- Stockfish nnue-pytorch(学習側の参考): https://github.com/official-stockfish/nnue-pytorch
- floodgate棋譜: http://wdoor.c.u-tokyo.ac.jp/shogi/
